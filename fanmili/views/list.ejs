<%-include header.ejs%>

<h1><%= title %></h1>
<ul>
    <% for(var i = 0; i < telList.length;i++){ %>
    <li>
        <%= telList[i].name %> <a href="tel:<%= telList[i].tel %>"><%= telList[i].tel %></a>
    </li>
    <% } %>
</ul>

<%-include footer.ejs%>